let counter=function(arr){
    return "There are "+arr.length+" elements in the array";
};
let adder=function(a,b){return `${a}+${b}=${a+b}`;};
const pi=Math.acos(-1);
module.exports= {
    counter,adder,pi,
}